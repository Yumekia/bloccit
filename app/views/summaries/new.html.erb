<h1>New Summary</h1>
 
 <div class="row">
   <div class="col-md-4">
     <p>Guidelines for summaries:</p>
     <ul>
       <li>Make sure it is a summary.</li>
       <li>No long-winded story.</li>
       <li>Get to the point.</li>
     </ul>
   </div>
   <div class="col-md-8">
     <%= form_for @summary do |f| %>
       <div class="form-group">
         <%= f.label :post %>
         <%= f.text_field :post, class: 'form-control', placeholder: "Enter summary post" %>
       </div>
       <div class="form-group">
         <%= f.label :body %>
         <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter summary                      body" %>
       </div>
       <div class="col-md-4">
       <%= f.submit "Save", class: 'btn btn-success' %>
     <% end %>
   </div>
 </div>
